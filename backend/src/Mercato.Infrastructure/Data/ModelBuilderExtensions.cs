using Microsoft.EntityFrameworkCore;

namespace Mercato.Infrastructure.Data;

public static class ModelBuilderExtensions
{
    public static void ConfigureMercatoModels(this ModelBuilder builder)
    {
        // Entity configurations will be registered here as modules grow.
    }
}
